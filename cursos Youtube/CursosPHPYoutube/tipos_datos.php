<?php

    //boolean (True = 1, False = 0)
    echo TRUE;

    //Enteros (positivos o negativos sin decimal)
    echo 10;
    //imprime por pantalla de que tipo es (boolean, entero, string...)
    var_dump(10);

    //float (con decimales)
    var_dump(7.5);

    //string (con el var_dump te dice cuantos carácteres contiene)
    var_dump("Hola Mundo");


?>